
const sounds = {};
const fullSoundMap = {};

export function playSound(name, { loop = false, volume = 1, game = 'common' } = {}) {
    // All sounds have been removed.
}

export function stopSound(name, { game = 'common' } = {}) {
    // All sounds have been removed.
}
